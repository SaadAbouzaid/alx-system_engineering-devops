#!/bin/bash

for file in *
do
    if [[ -f $file ]]
    then
        filetype=$(file -b --mime-type "$file")
        if [[ $filetype == "inode/x-empty" ]]
        then
            echo "$file: $filetype"
        else
            echo "$file: $(file -b "$file")"
        fi
    fi
done
